import { IOptions } from 'js2xmlparser/lib/options';
import { CustomObject, CustomField } from 'jsforce/api/metadata';
import { JsonMap } from '@salesforce/ts-types';
interface WriteJSONasXMLInputs {
    json: CustomField | CustomObject | JsonMap;
    type: string;
    options?: IOptions;
}
declare const convertJsonToXml: ({ json, type, options }: WriteJSONasXMLInputs) => string;
declare const parseXml: <T>(fileString: string, mainNodeName: string) => T;
export { convertJsonToXml, parseXml };
