import { ComputeEnvironment } from '../lib/sfdc-types';
export declare class Git {
    redacted: string[];
    constructor(redacted?: string[]);
    exec(commands: string[], quiet?: boolean): Promise<any>;
    status(): Promise<any>;
    hasUnpushedFiles(): Promise<any>;
    getCurrentBranch(): Promise<any>;
    getRemote(app: ComputeEnvironment, token?: string, username?: string): Promise<string>;
}
export default Git;
