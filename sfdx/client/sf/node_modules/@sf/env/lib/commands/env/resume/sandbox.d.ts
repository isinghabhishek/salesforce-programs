import { Org, SandboxProcessObject } from '@salesforce/core';
import { Duration } from '@salesforce/kit';
import * as Interfaces from '@oclif/core/lib/interfaces';
import { SandboxCommandBase } from '../../../shared/sandboxCommandBase';
declare type CmdFlags = {
    wait: Duration;
    name: string;
    'job-id': string;
    'target-org': Org;
    'use-most-recent': boolean;
};
export default class ResumeSandbox extends SandboxCommandBase<SandboxProcessObject> {
    static summary: string;
    static description: string;
    static examples: string[];
    static flags: Interfaces.FlagInput<CmdFlags>;
    static readonly state = "beta";
    protected readonly lifecycleEventNames: string[];
    private flags;
    run(): Promise<SandboxProcessObject>;
    protected getCheckSandboxStatusParams(): string[];
    private createResumeSandboxRequest;
    private resumeSandbox;
    private buildSandboxRequestCacheEntry;
    private verifyIfAuthExists;
    private getSandboxProcessObject;
}
export {};
