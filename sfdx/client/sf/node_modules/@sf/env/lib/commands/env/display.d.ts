import { SfCommand, JsonObject } from '@salesforce/sf-plugins-core';
export default class EnvDisplay extends SfCommand<JsonObject> {
    static readonly summary: string;
    static readonly description: string;
    static readonly examples: string[];
    static flags: {
        'target-env': import("@oclif/core/lib/interfaces").OptionFlag<string>;
    };
    run(): Promise<JsonObject>;
}
