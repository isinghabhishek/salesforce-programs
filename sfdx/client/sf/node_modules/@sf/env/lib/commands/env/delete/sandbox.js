"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/*
 * Copyright (c) 2021, salesforce.com, inc.
 * All rights reserved.
 * Licensed under the BSD 3-Clause license.
 * For full license text, see LICENSE.txt file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
const core_1 = require("@salesforce/core");
const sf_plugins_core_1 = require("@salesforce/sf-plugins-core");
core_1.Messages.importMessagesDirectory(__dirname);
const messages = core_1.Messages.loadMessages('@salesforce/plugin-env', 'delete_sandbox');
class EnvDeleteSandbox extends sf_plugins_core_1.SfCommand {
    async run() {
        const flags = (await this.parse(EnvDeleteSandbox)).flags;
        const org = flags['target-org'];
        if (!(await org.isSandbox())) {
            throw messages.createError('error.isNotSandbox', [org.getUsername()]);
        }
        if (flags['no-prompt'] || (await this.confirm(messages.getMessage('prompt.confirm', [org.getUsername()])))) {
            try {
                await org.delete();
                this.logSuccess(messages.getMessage('success', [org.getUsername()]));
            }
            catch (e) {
                if (e instanceof Error && e.name === 'SandboxNotFound') {
                    this.logSuccess(messages.getMessage('success.Idempotent', [org.getUsername()]));
                }
                else {
                    throw e;
                }
            }
            return { username: org.getUsername(), orgId: org.getOrgId() };
        }
    }
}
exports.default = EnvDeleteSandbox;
EnvDeleteSandbox.summary = messages.getMessage('summary');
EnvDeleteSandbox.description = messages.getMessage('description');
EnvDeleteSandbox.examples = messages.getMessages('examples');
EnvDeleteSandbox.flags = {
    'target-org': sf_plugins_core_1.Flags.requiredOrg({
        summary: messages.getMessage('flags.target-org.summary'),
        char: 'o',
    }),
    'no-prompt': sf_plugins_core_1.Flags.boolean({
        char: 'p',
        summary: messages.getMessage('flags.no-prompt.summary'),
    }),
};
EnvDeleteSandbox.state = 'beta';
//# sourceMappingURL=sandbox.js.map