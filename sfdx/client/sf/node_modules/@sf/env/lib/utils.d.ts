import { JsonObject } from '@salesforce/sf-plugins-core';
export declare function toValue(val: JsonObject[keyof JsonObject]): string | boolean | number;
export declare function toKey(key: string, translations?: Record<string, string>): string;
