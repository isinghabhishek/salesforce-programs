"use strict";
/*
 * Copyright (c) 2021, salesforce.com, inc.
 * All rights reserved.
 * Licensed under the BSD 3-Clause license.
 * For full license text, see LICENSE.txt file in the repo root or https://opensource.org/licenses/BSD-3-Clause
 */
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@salesforce/core");
const hook = async function (opts) {
    const orgs = await core_1.AuthInfo.listAllAuthorizations();
    const data = orgs.find((org) => org.username === opts.targetEnv) ?? orgs.find((org) => org.aliases?.includes(opts.targetEnv));
    if (data)
        delete data['timestamp'];
    return {
        data,
        keys: { orgId: 'Org ID', oauthMethod: 'Auth Method' },
    };
};
exports.default = hook;
//# sourceMappingURL=envDisplay.js.map