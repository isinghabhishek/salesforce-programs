import { SfCommand } from '@salesforce/sf-plugins-core';
export declare type LogoutResponse = {
    successes: string[];
    failures: string[];
};
export default class Logout extends SfCommand<LogoutResponse> {
    static readonly summary: string;
    static readonly description: string;
    static readonly examples: string[];
    static flags: {
        'no-prompt': import("@oclif/core/lib/interfaces").BooleanFlag<boolean>;
    };
    run(): Promise<LogoutResponse>;
    private promptForEnvironments;
}
