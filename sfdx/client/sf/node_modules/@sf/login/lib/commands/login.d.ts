import { AuthFields } from '@salesforce/core';
import { SfCommand } from '@salesforce/sf-plugins-core';
export declare enum LoginTarget {
    ORG = "Salesforce Org",
    FUNCTIONS = "Salesforce Functions"
}
export default class Login extends SfCommand<AuthFields> {
    static readonly summary: string;
    static readonly description: string;
    static readonly examples: string[];
    static enableJsonFlag: boolean;
    static flags: {};
    run(): Promise<AuthFields>;
    executeOrgLogin(): Promise<AuthFields>;
    executeFunctionsLogin(): Promise<AuthFields>;
    private promptUserToChooseLoginTarget;
    private promptUserForOrgSideEffects;
}
